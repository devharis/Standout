@model List<Supplier>
@using SWTS.Models
@{
    ViewBag.Title = "All suppliers";

}
<div class="ui-menu" id="leftMenu">
    <div class="accordion-group">
        <div class="accordion-heading">
            <a class="dropdown-toggle" data-toggle="collapse" href="#collapseOne">
                <i class="glyphicon ui-icon-gear glyphicon-home"></i> @Category.Diary
            </a>
        </div>
        <div id="collapseOne" class="collapse">
            <div class="subMenu">
                <ul>
                    @foreach (var item in Model.Where(item => item.Category == Category.Diary.ToString()))
                    {
                        <li>@Html.ActionLink(item.Name, "Supplier", "Home", new { id = item.SupplierId }, null)</li>
                    }
                </ul>
            </div>
        </div>
    </div>
    <div class="accordion-group">
        <div class="accordion-heading">
            <a class="dropdown-toggle" data-toggle="collapse" data-parent="#leftMenu" href="#collapseTwo">
                <i class="glyphicon glyphicon-th"></i> @Category.Bread
            </a>
        </div>
        <div id="collapseTwo" class="collapse">
            <div class="subMenu">
                <ul>
                    @foreach (var item in Model.Where(item => item.Category == Category.Bread.ToString()))
                    {
                        <li>@Html.ActionLink(item.Name, "Supplier", "Home", new { id = item.SupplierId }, null)</li>
                    }
                </ul>
            </div>
        </div>
    </div>
    <div class="accordion-group">
        <div class="accordion-heading">
            <a class="dropdown-toggle" data-toggle="collapse" data-parent="#leftMenu" href="#collapseThree">
                <i class="glyphicon glyphicon-leaf"></i> @Category.Meat
            </a>
        </div>
        <div id="collapseThree" class="collapse">
            <div class="subMenu">
                <ul>
                    @foreach (var item in Model.Where(item => item.Category == Category.Meat.ToString()))
                    {
                        <li>@Html.ActionLink(item.Name, "Supplier", "Home", new { id = item.SupplierId }, null)</li>
                    }
                </ul>                 
            </div>
        </div>
    </div>
    <div class="accordion-group">
        <div class="accordion-heading">
            <a class="dropdown-toggle" data-toggle="collapse" data-parent="#leftMenu" href="#collapseFour">
                <i class="glyphicon glyphicon-star"></i> @Category.Confectionery
            </a>
        </div>
        <div id="collapseFour" class="collapse">
            <div class="subMenu">
                <ul>
                    @foreach (var item in Model.Where(item => item.Category == Category.Confectionery.ToString()))
                    {
                        <li>@Html.ActionLink(item.Name, "Supplier", "Home", new { id = item.SupplierId }, null)</li>
                    }
                </ul>                 
            </div>
        </div>
    </div>
    <div class="accordion-group">
        <div class="accordion-heading">
            <a class="dropdown-toggle" data-toggle="collapse" data-parent="#leftMenu" href="#collapseFive">
                <i class="glyphicon glyphicon-cog"></i> @Category.Tobacco
            </a>
        </div>
        <div id="collapseFive" class="collapse">
            <div class="subMenu">
                <ul>
                    @foreach (var item in Model.Where(item => item.Category == Category.Tobacco.ToString()))
                    {
                        <li>@Html.ActionLink(item.Name, "Supplier", "Home", new { id = item.SupplierId }, null)</li>
                    }
                </ul>                 
            </div>
        </div>
    </div>
    <div class="accordion-group">
        <div class="accordion-heading">
            <a class="dropdown-toggle" data-toggle="collapse" data-parent="#leftMenu" href="#collapseSix">
                <i class="glyphicon glyphicon-file"></i> @Category.Cosmetics
            </a>
        </div>
        <div id="collapseSix" class="collapse">
            <div class="subMenu">
                <ul>
                    @foreach (var item in Model.Where(item => item.Category == Category.Cosmetics.ToString()))
                    {
                        <li>@Html.ActionLink(item.Name, "Supplier", "Home", new { id = item.SupplierId }, null)</li>
                    }
                </ul>                 
            </div>
        </div>
    </div>
</div>


